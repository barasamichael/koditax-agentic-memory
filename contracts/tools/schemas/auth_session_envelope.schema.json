{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AuthSessionEnvelope",
  "description": "Canonical auth session envelope for login, refresh, introspection, logout, and trusted context propagation.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "session_id",
    "user_id",
    "tenant_id",
    "role",
    "delegation_context",
    "issued_at",
    "expires_at",
    "is_invalidated",
    "correlation_id"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "1.0.0"
    },
    "session_id": {
      "type": "string",
      "pattern": "^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$"
    },
    "user_id": {
      "type": "string",
      "pattern": "^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$"
    },
    "tenant_id": {
      "type": "string",
      "minLength": 1,
      "description": "Single-tenant baseline may use deterministic value (for example default_tenant)."
    },
    "role": {
      "type": "string",
      "enum": [
        "IndividualTaxpayer",
        "TaxAgent",
        "Accountant",
        "Administrator"
      ]
    },
    "issued_at": {
      "type": "string",
      "format": "date-time"
    },
    "expires_at": {
      "type": "string",
      "format": "date-time"
    },
    "is_invalidated": {
      "type": "boolean"
    },
    "correlation_id": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[a-f0-9]{64}$"
    },
    "delegation_context": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "is_delegated",
        "principal_user_id",
        "delegate_user_id",
        "delegation_id",
        "granted_at",
        "revoked_at"
      ],
      "properties": {
        "is_delegated": {
          "type": "boolean"
        },
        "principal_user_id": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$"
        },
        "delegate_user_id": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$"
        },
        "delegation_id": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$"
        },
        "granted_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "revoked_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        }
      }
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "invalidated",
        "expired"
      ]
    }
  }
}
